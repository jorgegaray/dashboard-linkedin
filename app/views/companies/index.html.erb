<div class="box box-primary">
  <div class="box-header">
    <i class="fa fa-group"></i>
    <h3 class="box-title">Compañias</h3>
  </div>
  <!-- /.box-header -->
  <div class="box-body table-responsive">
    <table data-datatable="true" class="table table-striped table-bordered" cellspacing="0" width="100%">
      <thead>
      <tr>
        <th>Empresa</th>
        <th>Tamaño</th>
        <th>Pais</th>
      </tr>
      </thead>
      <tfoot>
      <tr>
        <th>Empresa</th>
        <th>Tamaño</th>
        <th>Pais</th>
        <th></th>
      </tr>
      </tfoot>
      <tbody>
        <% Company.all.each do |company| %>
          <tr>
            <td><%= company.name %></td>
            <td><%= company.size %></td>
            <td><%= company.country %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <br>
  </div>
  <div class="box-footer">
    <%= link_to 'Crear', new_company_path, class: "btn btn-default pull-right" %>
  </div>

  <!-- /.box-body -->
</div>

<% content_for :title do 'Compañias' end %>

  
  
  
  
